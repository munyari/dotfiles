[user]
	email = fundirap@gmail.com
	name = Panashe M. Fundira
	signingkey = 51BF74B5
[color]
	ui = auto
[core]
	editor = nvim
	excludesfile = /home/nash/.gitignore_global
	pager = less -F
[merge]
	tool = vimdiff
	ff = only
[alias]
	a = add
	aa = add --all
	ap = add --patch
	b = branch
	bd = branch -d
	browse = "!hub browse"
	can = commit --amend --no-edit
	ci = commit
	co = checkout
	cob = checkout -b
	compare = "!hub compare"
	d = diff
	dc = diff --cached
	glog = log -E -i --grep
	l = log --oneline --decorate --graph --all -20
	last = log -1 HEAD
	ls = log --oneline --graph --decorate --simplify-by-decoration
	mup = "!git checkout master && git pull && git checkout -"
	p = push
	pr = pull-request
	ri = rebase -i
	su = stash -u
	unchange = checkout --
	uncommit = reset --soft HEAD^
	unstage = reset HEAD --
	vdf = difftool
[push]
	default = upstream
[github]
	user = munyari
[commit]
	gpgsign = true
[fetch]
	prune = true
[gitsh]
	noGreeting = true
	defaultCommand = status --short --branch && :echo
[gitsh "color"]
	default = cyan
	untracked = yellow
	modified = red
[merge]
	tool = vimdiff
[mergetool "vimdiff"]
	cmd = "nvim -d  \"$LOCAL\" \"$REMOTE\""
